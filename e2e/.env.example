# E2E Test Environment Configuration
# Copy this file to .env in the e2e directory and update with your actual test credentials

# Test Admin User Credentials
# These credentials should match a real admin user in your test database
TEST_ADMIN_USERNAME=admin
TEST_ADMIN_PASSWORD=1234
TEST_ADMIN_EMAIL=admin@example.com

# Test Regular User Credentials
# These credentials should match a real regular user in your test database
TEST_USER_USERNAME=user_test
TEST_USER_PASSWORD=1234
TEST_USER_EMAIL=user@example.com

# Test Registration Password
# This password will be used when creating new test users during registration tests
TEST_REGISTRATION_PASSWORD=TestUser@123

# API Base URL for E2E Tests
# This is the backend API URL that the frontend will connect to during tests
API_BASE_URL=http://localhost:8080

# Mock API Toggle
# Set to 'true' to use mocked API responses (fast, isolated tests)
# Set to 'false' to use real API (integration tests)
USE_MOCK_API=false

